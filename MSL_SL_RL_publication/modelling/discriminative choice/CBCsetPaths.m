function options = CBCsetPaths(options, startdir, start, analysisdir, analysis)
% input:    options, startdir, start, analysisdir, analysis
% output:   updated options 

options.startdir = startdir;
options.start = start;
options.analysisdir = analysisdir; 
options.analysis = analysis;

end
